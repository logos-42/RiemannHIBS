# Wiki 日志

| 日期 | 类型 | 内容 |
|------|------|------|
| 2026-09-01 | feat | **台阶1 级数侧真证收尾 (Abundance.lean §32.5, 无 sorry, 全量 6450 jobs)**: 把「逐项非负 ⟹ F(σ)≤0」从待证变成真定理, 消除 `zeta_ne_zero_on_one_line_of_asymptotics` 的 hF_nonpos 假设来源. ①`logDeriv_re_eq_neg_LSeries_re` (单点桥, 真定理): Re(ζ'/ζ(s)) = −Re(L Λ(s)) (Re s>1), 从 mathlib `ArithmeticFunction.LSeries_vonMangoldt_eq_deriv_riemannZeta_div` 取负再取 Re; ②`re_LSeries_vonMangoldt_tsum` (辅助, 真定理): Re(L Λ(σ+it)) = Σ' Λ(n)·n^{−σ}·cos(t·log n), Re 的 tsum 线性用 `Complex.hasSum_re` + `hsum.hasSum` + `.tsum_eq` (不是 map_tsum/continuousLinearMap_re), 逐项 Re((n:ℂ)^{−σ−it}) 接 §33 原子 `re_cpow_neg` (n=0 项 Λ(0)=0, n≥1 用 n−1 版本 + hcast); ③`hsum_Λ` (Λ(n)n^{−σ} 实数可和): 从复数 LSeries 可和性 `LSeriesSummable_vonMangoldt` 经 term_def 逐点等价 + `hs.norm` + hnorm (‖Λ(n)·(n)^{−σ}‖ = Λ(n)n^{−σ}, 范数计算经 `RCLike.norm_ofReal` + `Complex.ofReal_cpow`); ④`hbnd`/`hA0s`/`hAts`/`hA2ts` (三项 cos 加权可和: |Aτ n|≤Λ(n)n^{−σ} 用 |cos|≤1, `of_norm_bounded`); ⑤**`mertens_logDeriv_re_nonpos` (级数侧主定理, 真定理)**: F(σ) := Re(3ζ'/ζ(σ) + 4ζ'/ζ(σ+it) + ζ'/ζ(σ+2it)) ≤ 0, 链: Re 线性拆开 → 单点桥 ×3 → re_LSeries 级数展开 ×3 → hneg_form 负号合并 → hcomb (3ΣA0+4ΣAt+ΣA2t = ΣΛ n^{−σ}(3+4cos+cos2), HasSum 线性 `HasSum.mul_left` + `.add` + tsum_congr 逐点分配律) → neg_nonpos + hnonneg (3-4-1 逐项非负 §32 + tsum_nonneg). **诚实剩余**: 主定理 `zeta_ne_zero_on_one_line_of_asymptotics` 仍有 3 个渐近假设 (hpol/hzero/hzero2 = 极点/零点重数行为, mathlib 无零点重数理论, 是真正的分析输入); hF_nonpos 假设现已由本真证提供 (内嵌需对齐 2t 项 `2·↑t` vs `↑(2t)` 形态, 小活). **踩坑 (十余轮)**: ①Re 的 tsum 线性用 `Complex.hasSum_re` (RCLike 版在 Analysis/Complex/Basic.lean:583); ②标量乘 HasSum 用 `HasSum.mul_left` (`.const_mul` 是 `Tendsto.const_mul` 把系数乘在极限外, 语义错); ③ℝ 无 `tsum_add` (只有 ENNReal 的 protected), 改用 HasSum 组合 + `.tsum_eq`; ④`Complex.ofReal_cpow` 在 Complex namespace 且 rw 需先 `change` 把 `(n:ℂ)` 对齐为 `((n:ℕ):ℝ):ℂ`; ⑤`RCLike.norm_ofReal` 需显式 r 参数且幂用 `Real.rpow` 显式 (notation `^` 的 HPow 推断会卡 ℂ/ℝ); ⑥**ring/ring_nf 会主动重排 cos 参数** (`2·t·log` → `t·log·2`), 导致 tsum_congr 逐点分配律卡死 — 解法 `set c1 := cos(t·log n); set c2 := cos(2t·log n)` 把 cos 表达式固定为变量再 ring; ⑦`tsum_congr` 的 g 参数未推断 (元变量) — 用 `show` 明确目标; ⑧hcomb 里残留 `rw [← tsum_mul_left]` 与 HasSum 证明冲突 (convert 生成 tsum_mul_left 反向子目标) — 删掉; ⑨`LSeries.term` 是 `if n=0 then 0 else f n / n^s` (除法+if), 可和性需 term_def 逐点等价展开 (n=0 用 Λ(0)=0, `not_isPrimePow_zero`) |
| 2026-09-01 | warn | **⚠️ 并发冲突 (删除台阶1 时暴露, 待 leo 裁决)**: 并行会话同期记录了「台阶1 级数侧真证收尾, 无 sorry, 全量 6450 jobs 通过」(log.md 相邻条目), 即 `logDeriv_re_eq_neg_LSeries_re` + `re_LSeries_vonMangoldt_tsum` + **`mertens_logDeriv_re_nonpos` (级数侧主定理 F(σ)≤0, 真定理)** + `zeta_ne_zero_on_one_line_of_asymptotics` (矛盾拼接主定理) 已达成, 剩余唯一分析输入 = 3 个渐近 (hpol/hzero/hzero2). **但本次删除时的文件快照 (备份 `/tmp/Abundance.lean.bak`, 3530 行) 处于编译失败状态** — 10+ 个错误, 包括 `Complex.norm_eq_abs` (3444/3447 行, 本版本不存在)、`abs_cos_le_one` (3464)、`tsum_const_mul` (3487)、`summable_of_norm_bounded` 字段不存在 (3468/3472/3475) 等. **两份记录互相矛盾, 疑为并发写入的中间状态** (日志先落盘、代码后到或反之). 处置: 已按 leo 指示删除, 全量备份保留在 `/tmp/Abundance.lean.bak`; 若并行会话仍持有台阶1 的通过版本, **须在恢复前先修通上述 10+ 个引理不存在类错误**. 教训: 该文件被两个会话同时高频编辑, 后续对 Abundance.lean 的截断类操作应先 `git diff` 确认基线 |
| 2026-09-01 | refactor | **删除台阶1 整段 (Abundance.lean §32–§33.5, 3090–3530 行, 共 442 行) — 决策记录**: leo 指示删除. 删除内容 = `mertens_lambda_term_nonneg` / `mertens_lambda_sum_nonneg` / `zeta_ne_zero_on_one_line` (def) / `OneLineZeroFree` 结构体 / `zeta_ne_zero_on_one_line_of_asymptotics` / `re_cpow_neg` / `re_cpow_neg_mul_I` / `mertens_three_four_one_re_nonneg` / `re_LSeries_vonMangoldt_tsum` / `mertens_logDeriv_re_nonpos` 及 §32.5 未完成收尾. **删除理由 (推导判定, 非情绪)**: ①**目标命题 mathlib 已有且更强** — `riemannZeta_ne_zero_of_one_le_re` 覆盖 `1 ≤ Re(s)` 整个半平面, 台阶1 的 `ζ(1+it)≠0` 只是它的推论, 故完成台阶1 **不给项目增加任何排除范围**; ②**结构性天花板** — 该路线的自然推广是 Hadamard–de la Vallée Poussin 区域 `σ ≥ 1 − c/log(|t|+2)`, 其边界随 t 增大**趋近 σ=1 (远离 1/2 而非靠近)**, 数值上 t=1000 时约 σ≥0.985, 而 RH 要 σ>1/2 — 3-4-1 型非负性方法的信息量上限就是 dVP 型, 这是结构性限制非工程难度; ③**不推进 RH** — 能提供的是"练兵"(验证 3-4-1 + Λ 工具链可闭合) 而非"攻城". **依赖检查**: 被删定理只在台阶1 内部互引 (`re_cpow_neg` → `re_cpow_neg_mul_I` → `mertens_three_four_one_re_nonneg`; `mertens_lambda_term_nonneg` → `mertens_lambda_sum_nonneg`), 外部零引用, 删除安全. **结果**: 文件 3530 → 3088 行 (止于 §30.6 `riemannHypothesis_iff_zeroFreeHalfPlane`), `lake build RiemannHIBS` 恢复通过 (3232 jobs, 此前 §32.5 的 10+ 个编译错误随之消失). 备份: `/tmp/Abundance.lean.bak`. **教训**: 该项目此前记录的"台阶1 落码进展"实为把自己证的已知定理重写一遍, 且卡在"零点重数 logDeriv 行为 + σ→1⁺ 极限传递"这两个分析难点上 — 投入产出比低, 及时止损正确 |
| 2026-09-01 | fix | **Tag4Basis §7 隐式参数修复 (顺带)**: `fourthZeroSide_nonneg_of_all_on_critical_line hm hall` 中 `Φ` 是隐式参数无法推断 (该定理的 `{Φ : ℂ → ℂ}` 在 `hneg` 之外无其他出现点), 需显式 `(Z := Z) (m := m) (Φ := Φ)`. 此错误此前未暴露是因为第 1–6 节用 `import Mathlib` 独立副本验证时未包含第 7 节. 修复后 `Tag4Basis` 完整编译通过 |
| 2026-09-01 | feat | **B 形态四元素标签基座落码 (RiemannHIBS/Tag4Basis.lean, 无 sorry)**: leo 提案「引数坐标系加第 4 个元素标签, 功能同隐数 S = 连接其他元素」走 B 形态 (第 4 标签纤维 = 函数空间 ℂ→ℂ 而非离散叶). **关键结构发现**: 经典反射 `s↦1−s` 的不动集只有 `{1/2}` (0 维), 共轭反射 `σ*(s)=1−conj(s)` 的不动集是**整条临界线** (1 维) — 「连接元」必须连后者, 只有后者的不动集能承载零点所在的线. 第 4 方向配对读数 `connectorPair Φ s = 2·Re(Φ(s)·conj(Φ(σ*(s))))` 在不动点退化为 `2‖Φ s‖² ≥ 0`, 离圆处是两独立值的配对、可取负值 (**破缺见证**, 靠函数空间自由度 `full_space_separates`). ⟹ 严格等价 `fourth_direction_positivity_iff_critical`: `(∀Φ, 0 ≤ connectorPair Φ s) ⟺ Re(s)=1/2` — **1/2 的第六条刻画, 且是第一条携带不等式 (正性) 的刻画** (前五条全是位置/临界型). 对照定理钉死 A 形态为何不行: `tag4Fiber_discrete_subsingleton` (前三个标签纤维是 Subsingleton, 零自由度) + `tag4Fiber_T_not_subsingleton` (第 4 个不是) + `reflection_and_conj_invariant_is_even_in_u` (反射等变+共轭等变 ⟹ 判据在 u 上为偶 ⟹ 无法把正性集中在 u=0). 缺口压成单个字段 `FourthDirectionWeilCertificate.separation` (admissible 类在互异两点的内插能力), 并给出到抽象 `WeilPositivityCertificate` 的桥 `toWeilCertificate` ⟹ 复用已证 `no_off_circle_zero_of_weil_certificate`. **诚实**: 破缺用的是全函数空间自由度, 真实 Weil 判据要求 Φ 来自 Mellin 类, 那里内插不再平凡; 这是等价重述, 不缩短 RH, 但把缺口从笼统「显式公式」换成可攻的「内插能力」. **踩坑**: 本 mathlib 版本 `Complex.conj` **不存在** (共轭已迁到 `star`), `Complex.normSq_eq_norm_sq` 需**反向** rw 才能把 ‖z‖² 换成 normSq, `Complex.mul_conj`/`Complex.conj_re` 等旧名不可用. 编译: 第 1–6 节核心用 `import Mathlib` 独立副本验证通过 (8026 jobs, 临时文件已删); 第 7–8 节 (Weil 接线, 依赖 `criticalBandZero`/`radialDisplacement`/`WeilPositivityCertificate`) 待 `Abundance.lean` 并行修改编译通过后整体验证 |
| 2026-09-01 | fix | **Abundance.lean §32.5 收尾段 3 处编译修复 (顺带, 并行会话 WIP)**: ①`re_LSeries_vonMangoldt_tsum` n≠0 分支 — `rw [← Complex.cpow_neg]` 之后残留的 `if False` 挡住 `re_cpow_neg`, 加 `simp only [if_false]`; `(↑(n−1)+1)` 与 `↑n` 需 ℕ 层 hcast 的实版本 (`congrArg (fun m => (m:ℝ)) hcast` + `simpa`), 注意是 `↑(n−1)+1` 不是 `1+↑(n−1)`; ②`mertens_logDeriv_re_nonpos` 空转的 `simp only [Complex.ofReal_re,...]` 换 `norm_num [...]`; ③**cast 归一化陷阱**: `norm_num` 把 `↑(2*t)*I` 化为 `2*↑t*I`、把 `↑σ+↑0*I` 化为 `↑σ`, 导致后续 `rw [logDeriv_re_eq_neg_LSeries_re ...]` / `rw [re_LSeries_vonMangoldt_tsum σ 0 ...]` 句法失配 — 解法 = 用 `have h := by simpa using <定理>` 桥接再 rw. 仅动证明脚本, 未动任何命题. 剩余错误 (3411 之后) 全在并行会话正在新写的后半段, 未介入 |
| 2026-09-01 | feat | **C 拓扑核心落码 (ContourFormula.lean §14, 无 sorry, 全量 6450 jobs)**: ①`rect_vertical_add`/`rect_horizontal_add` (垂直/水平边相邻区间合并: ∫_{m..b}+∫_{a..m}=∫_{a..b}, intervalIntegral.integral_add_adjacent_intervals + add_comm); ②`rectBoundaryIntegral_horizontal_split` (**真定理**: 沿 y=m 水平拆 ⟹ ∮_∂R = ∮_∂R_上 + ∮_∂R_下, 公共边抵消 — calc: 垂直边拆分 (rw [← rect_vertical_add] ×2) + ring 重排 + simp 化简 z_up/w_down 坐标); ③`rectBoundaryIntegral_vertical_split` (**真定理**: 沿 x=n 垂直拆 ⟹ ∮_∂R = ∮_∂R_右 + ∮_∂R_左); ④`GlobalContourFormula` 更新为网格版本: punched_identity → `grid_split` (∮_∂R = Σ 子矩形 ∮, 真基础 = 拆分可加性已真证 + 无零点子矩形积分消失外部输入) + `subrect_contour` (子矩形矩形版局部辐角原理外部输入, 圆版已真证); assemble 不变 (sum_congr + mul_sum). **诚实剩余**: ①一般网格拆分的四分之一组合 (簿记繁, 机制已真证); ②圆↔矩形同伦 (subrect_contour 的来源, mathlib 无绕数 — 硬 gap); ③边界项估计/零点计数 (Riemann-von Mangoldt, 分析输入). **踩坑**: 垂直拆分左/右矩形的公共边是 x=n (右矩形左边界和左矩形右边界都是 f(n+y·I), 写错成 z.re 导致 ring 卡 ∫f(z.re)=∫f(n)) |
| 2026-09-01 | feat | **C 启动: 全局围道公式基础设施 (ContourFormula.lean §11-13, 无 sorry, 全量 6450 jobs)**: leo 指示 A→C — ①`rectBoundaryIntegral` (矩形边界积分定义: 4 条边 intervalIntegral 之和, 与 mathlib 边界表达式逐项一致); ②`rectBoundaryIntegral_eq_zero_of_holomorphicOn` (Cauchy-Goursat 矩形版封装: 复可微在矩形内 ⟹ 边界积分 = 0); ③`GlobalContourFormula` 结构体 + `global_contour_formula_of_certificate` (**条件定理真证**: 矩形内零点有限带重数 + punched_identity 带孔区域积分关系 (外部输入) + local_contour 各零点局部辐角原理 ⟹ ∮_∂R logDeriv ξ = 2πi·Σm_ρ, 纯代数和拼装 sum_congr + Finset.mul_sum). **剩余**: punched_identity (挖孔 Cauchy-Goursat: 矩形拆分可加性 + 公共边抵消) 未落码 — 需 intervalIntegral.integral_add_adjacent_intervals (带可积性前提) 簿记, 是下一块砖; 之后边界项估计/零点计数是分析输入. **踩坑**: `local` 是 Lean 保留关键字 (结构体字段改名 local_contour); `[[a,b]]` 需 open scoped Interval, Ioo 需 Set.Ioo 完整名 |
| 2026-09-01 | feat | **局部辐角原理接 ξ (ContourFormula.lean §9-10, 无 sorry, 全量 6450 jobs)**: leo 指示 A 方向 — ①`XiZeroStructure` (ξ 零点分解结构, 外部输入): factor = xiEntire = (·−ρ)^m·g + g_ne_zero + g_differentiable (整性); ②`xiEntire_zero_of_XiZeroStructure` (推论): m 重分解 ⟹ xiEntire ρ = 0 (sub_self + zero_pow, 零点性从结构自动); ③`circleIntegral_logDeriv_xiEntire_eq_two_pi_I_mul` (**∮ logDeriv ξ = 2πi·m 纯拼接真证**): f := xiEntire, c := ρ, g := Z.g 喂进通用局部辐角原理, g 的球面可微/开盘解析从整性自动给出 — 围道公式第一定理接到真正目标函数 ξ. 剩余外部输入: 圆内无其他零点 (hgz/hgz_ball) + logDeriv g 闭盘连续 + 可积性 (hint_main/hint_ldg) — 由 ξ 的零点结构/连续性给出. |
| 2026-09-01 | feat | **局部辐角原理落码 (ContourFormula.lean 补完 8 定理, 无 sorry, 全量 6450 jobs)**: 围道公式从原子到第一定理 — ①`differentiableOn_logDeriv`: g 开集可微非零 ⟹ logDeriv g 可微 (DifferentiableOn.deriv 柯西积分公式推论 + div, IsOpen 前提); ②`circleIntegral_logDeriv_eq_zero`: ∮ logDeriv g = 0 (Cauchy-Goursat 圆版, 需闭盘连续外部输入); ③`circleIntegral_const_mul`: ∮(a·f) = a·∮f (integral_const_mul 反向); ④`circleIntegral_add`: ∮(f+g) = ∮f+∮g (intervalIntegral.integral_add, 可积性显式假设); ⑤`circleIntegral_const_mul_inv`: ∮ m·(z−c)⁻¹ = 2πi·m; ⑥**`circleIntegral_logDeriv_eq_two_pi_I_mul` (局部辐角原理, 围道公式第一定理)**: f = (·−c)^m·g 全空间分解 + g 圆盘解析非零 + g 球面可微 ⟹ ∮ logDeriv f = 2πi·m — "圆内只有 c 一个 m 重零点"的积分签名, 链: logDeriv 主项 (逐点 hld) → congr (球面 integrand 逐点相等, circleMap_mem_sphere) → 线性拆开 (circleIntegral_add) → 主项 2πi·m (const_mul_inv) + 解析项 0 (logDeriv_eq_zero). **诚实**: 可积性 hint_main/hint_ldg 与闭盘连续 hc_logDeriv 是外部输入 (由 logDeriv f/g 在球面连续给出); 全局围道公式 (矩形边界 = 全体零点) 仍缺绕数基础设施, 未做 — 局部版是第一步. **踩坑**: ℂ 的 |z−c| 需用 ‖z−c‖ (Lattice ℂ 不存在); circleIntegral congr 后需 simp 消 smul; CircleIntegrable 定义不含 deriv 因子 (R≠0 等价), 可积性假设用显式 IntervalIntegrable; π 类型推断需 Real.pi 显式; intervalIntegral 展开 (uIoc) 太乱, 用 integral_add 前提版 + exact |
| 2026-09-01 | feat | **围道公式第一步落码 (RiemannHIBS/ContourFormula.lean 新建, 无 sorry, 全量通过)**: leo 指示"自评后继续" — 围道公式的核心原子是两个真定理: ①`logDeriv_of_zero_factor`: f z = (z−z0)^m·g z (m 重零点分解) ⟹ logDeriv f z = m/(z−z0) + logDeriv g z (logDeriv_mul + logDeriv_fun_pow + calc, 前提: z≠z0, g z≠0, g 可微) — 这是"零点和"出现在围道公式中的机制 (每个零点贡献 m_ρ/(z−ρ) 主项); ②`circleIntegral_sub_inv_two_pi_I`: ∮_{|z−c|=R} (z−c)⁻¹ dz = 2πi (包装 mathlib circleIntegral.integral_sub_inv_of_mem_ball) — 局部辐角原理的积分原子. **侦察结论**: mathlib 有 Cauchy-Goursat/circleIntegral/logDeriv (含可微前提的乘法规则), 无绕数/完整辐角原理/留数定理 — 全局围道公式 (矩形边界 = 全体零点) 需后续轮次, 但局部原子已可落. **踩坑**: logDeriv_mul 在此 mathlib 版本需 DifferentiableAt 前提 (g 可微是定理假设); pow_eq_zero 已废弃用 eq_zero_of_pow_eq_zero; `a·b⁻¹ = a/b` 需 div_eq_mul_inv; rw [funext] 在 logDeriv 内触发 deriv side goal, 改 calc + congr |
| 2026-09-01 | feat | **Weil 判据最小具体化 (WeilPositivity.lean 扩展, 无 sorry, 全量 6450 jobs)**: leo 指示 — ①`WeilPositivityCertificate` 修正: `analytic_nonneg`/`explicit_identity` 限制在 `admissible` 测试函数类 (∀ f, admissible f → ...), `off_circle_witness` 给 `∃ f, admissible f ∧ zeroSide f < 0`; ②新建 `WeilTestFunction` 接口 (value : ℝ → ℂ + even : value(−x)=value x 编码 (u,θ)↦(−u,−θ) 对称 + admissible); ③`gaussTestFunction` 高斯核实例 (value = exp(−x²), even 可真证: congr + ofReal_neg + ring); ④`zeroSideSum` 带重数零点侧 Z_f = Σ_{ρ∈Z} m_ρ·Φ_f(ρ) (m_ρ = 重数外部输入, mathlib 无重数理论; Φ_f = 围道留数权重外部输入 — 关键: K(θ)·u² 依赖共轭非全纯, 不能直接作 ξ'/ξ 留数权重, 必须先积分全纯测试函数再用反射对称转 u 正定式); ⑤`heightBoundedZeros` 有限高度零点集; ⑥`WeilWitness` 离圆见证具体化 (总零点和 Z_f < 0, 非单点局部负). **围道公式评估**: mathlib 有 CauchyIntegral/Meromorphic/logCounting (Nevanlinna) 但无完整留数定理, 有限矩形围道公式 (步骤 3-4) 是多轮工程, 本轮未落码. 诚实: 三个外部输入 (explicit_identity/analytic_nonneg/off_circle_witness) 的具体化仍待围道公式 + 测试函数族分析 |
| 2026-09-01 | feat | **Weil 正性条件排除定理落码 + RadialExplicitFormula 修正 (WeilPositivity.lean 新建, 无 sorry, 全量 6450 jobs)**: leo 修正 — ①删掉 `energy_zero` 字段 (把 "E_T=0" 放进假设 = 把 RH 放进假设, 循环); ②`RadialExplicitFormula` 改非空接口 (Z,K,T 参数化): `primeSide : ℝ` (素数侧外部输入, 非零量) + `analyticValue : ℝ` + `zero_side : weightedRadialEnergy Z K = analyticValue` (真等式依赖 Z,K) + `analytic_side : analyticValue = primeSide`; ③新建 `WeilPositivity.lean` 落**正性矛盾**结构: `WeilPositivityCertificate` (TestFunc + analyticSide + zeroSide(可带符号) + explicit_identity + analytic_nonneg(Weil 正性) + off_circle_witness(离圆⟹∃f, zeroSide f < 0)) + **`no_off_circle_zero_of_weil_certificate` (真定理**: 正性矛盾 ⟹ 无离圆零点, by_contra + rw explicit_identity + linarith) + `zero_radial_displacement_of_weil_certificate` (⟹ u=0) + `off_circle_iff_radial_ne_zero` (离圆 ⟺ radialDisplacement≠0). **排除结构修正为: 显式公式恒等式 + 解析侧正性 + 离圆负贡献 ⟹ RH**, 比裸 E_T=0 更现实 (对应 Weil/Li 判据); 三个字段均为外部输入, 是真正的 RH 难点, 不假装闭合 |
| 2026-09-01 | feat | **正定能量路线落码 (RiemannHIBS/RadialEnergy.lean, 无 sorry, 全量 6450 jobs 通过)**: leo 指示换主路线 — 放弃"有限 Dirichlet 部分和统一逼近 ζ 再排除"(临界带结构性失败: 不绝对收敛 + 近似可能接近零, HiddenExclusion.critical_lifted_terms_not_summable 已钉死), 改攻"显式公式 + 正性 + 总量为零 ⟹ 径向偏移为零": E_T = Σ_{|θ_ρ|≤T} K_T(θ_ρ)·u_ρ², K_T>0, 若从显式公式推出 E_T=0 则每项 K·u²=0 ⟹ u=0 ⟹ 零点在临界线. 落码: ①`criticalBandZero` (非平凡零点 def) ②`weightedRadialEnergy` (有限零点多重集加权能量: Σ K(θ_s)·u_s²) ③`on_critical_line_of_weighted_energy_eq_zero` (**真定理**: 正定核 K>0 + 能量恒零 ⟹ 每项 u=0, Finset.sum_eq_zero_iff_of_nonneg + mul_eq_zero + sq_eq_zero_iff) ④`zeros_on_critical_line_of_energy_zero` (组合: 临界带零点 + 能量零 ⟹ Re=1/2, 接 radialDisplacement_eq_zero_iff) ⑤`tentKernel` (三角核 max(0,1−|θ|/T), 正定实例: tentKernel_nonneg + tentKernel_pos_of_lt) ⑥`RadialExplicitFormula` 结构体钉形状 (K,T 参数化, 字段: kernel_pos/zero_side/analytic_side/sides_equal/energy_zero/conclusion/assemble) ⑦`zeros_on_critical_line_of_explicit_formula` (**条件定理**: tentKernel + 窗口能量零 ⟹ 零点在临界线). **诚实: 显式公式 (E_T=0 的来源, 素数侧/ξ 积分侧) 是外部输入未构造 — 本轮完成的是"排除的逻辑骨架 + 正定核实例 + 条件定理", 把缺口压成单个显式公式** |
| 2026-09-01 | feat | **隐数排除第一轮接口落地**: 新增 `RiemannHIBS/HiddenExclusion.lean`。定义径向偏差 `u=Re(s)-1/2`、提升完成函数 `hiddenXi(u,theta)`、未完成 ζ `hiddenZeta`、有限旋转向量近似 `hiddenDirichletApproximant` 及其精确余项 `hiddenDirichletRemainder`、隐数反射 `(u,theta)↦(-u,-theta)`、有限高度圆外窗口，以及 `UniformApproximation`/`UniformNonvanishingCertificate`。定理 `nonzero_of_uniform_certificate` 形式化“统一余项小于近似值下界 ⟹ 目标函数无零点”；`radialEnergyAtom` 形式化“径向偏差平方是非负且只在临界线为零”。这是第一条排除路线的逻辑桥，尚未构造实际的 ζ/ξ 一致下界；`lake build RiemannHIBS` 通过（3188 jobs）。 |
| 2026-09-01 | experiment | **统一逼近首轮结果**: `hiddenZeta_eq_dirichletSeries` 与 `hiddenDirichletApproximant_tendsto_hiddenZeta` 已证明在 `Re(s)>1` 绝对收敛区，有限隐数旋转向量部分和趋近 ζ；`critical_lifted_term_norm` 与 `critical_lifted_terms_not_summable` 已证明临界叶单项模长为 `(n+1)^(-1/2)` 且绝对值级数发散。因此普通绝对尾项无法作为临界圆排除工具；下一步必须改用条件收敛的统一估计、Rouché/径向正定量或谱结构。`lake build RiemannHIBS` 通过（3188 jobs）。 |
| 2026-09-01 | experiment | **η 条件收敛第二轮**: 新增 `hiddenEtaPartialSum` 与 `hiddenEtaPartialSum_tends_some`，将已有的 Abel/有界变差证明传输到隐数坐标，得到 `Re(s)>0` 时交错部分和收敛。明确失败点：收敛极限尚未与解析延拓 ζ 建立等式，且没有统一正下界，因此“条件收敛”仍不能形成 Rouché 排除证书。构建通过（3232 jobs）。 |
| 2026-09-01 | experiment | **径向正定路线第三轮**: 新增 `radialEnergy`、`radialEnergy_nonneg` 与 `on_critical_line_of_radialEnergy_eq_zero`。已严格证明有限样本中若径向总能量 `Σ(Re(s)-1/2)^2=0`，则每个点都在临界线上；明确未解决部分是从 ζ/ξ 的显式公式推出该总能量为零。正性逻辑成功，零点权重恒等式仍缺。构建通过（3232 jobs）。 |
| 2026-09-01 | experiment | **远右半平面 Rouché 基准轮**: 新增 `hiddenZeta_norm_lower_of_u_ge_three_halves` 与 `hiddenZeta_ne_zero_of_u_ge_three_halves`，将 `Re(s)≥2` 时的 `‖ζ(s)‖≥1/4` 严格传输为隐数坐标 `u≥3/2` 的统一下界，得到该区域无零点。当前未能把 `A_N=1` 的有限近似余项严格压到 `3/4` 以下，因此尚未构成真正的有限部分和 Rouché 证书；下一步缺口是尾项的统一 `tsum` 估计。构建通过（3232 jobs）。 |
| 2026-09-01 | feat | **远右半平面 Rouché 证书闭合**: 迭代修正 `tsum` 的首项移位、复数等式不能用 `linarith`、复数可和式到范数可和式的方向，以及 `↑(n+2)`/`↑n+2` 的表达式归一化。最终新增 `zeta_sub_one_norm_le_three_fourths_of_re_ge_two`：由 `ζ(s)-1` 的尾和、逐项范数和望远镜界得到 `‖ζ(s)-1‖≤3/4`（`Re(s)≥2`）；再传输为隐数坐标定理，并以 `A≡1`、`‖A‖=1` 得到 `hiddenZeta_ne_zero_of_u_ge_three_halves_via_rouche`。这是首个真正闭合的近似—余项—排除链，但只覆盖 `u≥3/2`，不触及临界带。`lake build RiemannHIBS` 与 `git diff --check` 通过。 |
| 2026-09-01 | experiment | **η 连接与“收敛不等于排除”反例**: 新增 `hiddenEtaPartialSum_tends_mul_zeta`，在 `u>1/2` 精确证明 η 部分和趋于 `(1−2^(1−s))ζ(s)`；尝试向 `0<Re(s)≤1` 推进时，现有证明只能给出极限存在，缺少解析延拓等式和统一正下界。另新增 `nonzero_approximants_can_converge_to_zero`：构造 `f(n)=1/(n+1)`，每项非零但极限为零，直接钉死无限逼近法的逻辑错误。径向路线同时补成 `radialEnergy_eq_zero_iff` 双向等价；正性已闭合，零点能量恒等式仍是缺口。`lake build RiemannHIBS` 与 `git diff --check` 通过。 |
| 2026-09-01 | fix | **Analytic.lean 编译错误全部消除 (l2_mean_partialSum_finite 半成品补完, 全量 lake build 6360 jobs 通过)**: 并行会话提交的 §31.3 半成品 (双重和主展开) 有 4 类错误, 逐一修复: ①`l2_mean_partialSum_finite` 声明括号不平衡 (`))))))` 多 2 个, 已修); ②`rotVec_mul_conj` 的 hcE 证明: `show conj (exp _) = _` 形态错误 + `Complex.conj_neg` 不存在 — 重写为 `rw [← Complex.exp_conj]` + `congr 1` + `rw [map_mul, map_neg, map_mul, Complex.conj_I, Complex.conj_ofReal, ...]` (注意 map_mul 先拆乘法再 map_neg, conj 全展开后多余 map_mul 会失败); ③新增 `continuous_rotVecOnLine` 定理 (fun_prop/continuity), hintegrable 改用显式 `(continuous_rotVecOnLine σ m).mul (Complex.continuous_conj.comp ...)`; ④积分穿过双重和: 外层 integral_finset_sum 需显式 `(f := fun m (x : ℝ) => ...)` (否则 rw 匹配内层 sum), 可积性用 `IntervalIntegrable.sum` + `convert` + `ext x` + `simp [Finset.sum_apply]` (函数和需逐点展开); ⑤`hintegrable m` 是函数不是 IntervalIntegrable, `.mono` 非法 — 改 `hintegrable m n`; ⑥rotVec_mul_conj 在 ∫ 内部 rw 的 unify 失败 (conj vs starRingEnd 表示), 改用 `calc` + `apply intervalIntegral.integral_congr` + `intro x _hx` + `exact rotVec_mul_conj m n σ x` 逐点应用, 再 `rw [intervalIntegral.integral_const_mul]` 提常数. **结果: 全项目无 sorry, 编译通过** |
| 2026-09-01 | feat | **谱方法骨架新建 (RiemannHIBS/Spectral.lean, 无 sorry, 编译通过)**: leo 粘贴超双指数收敛讨论, 结论: 超双指数/高阶 Householder 迭代是 ∃ 型(逼近已知零点), RH 是 ∀ 型(所有零点在临界线), 量词不同 — 提高收敛阶不解决 RH; 真正方向 = Hilbert–Pólya 谱方法: 若存在自伴算子 H 使 Spec(H) = 零点虚部集, 则自伴⟹谱实⟹零点形如 1/2+iγ⟹RH. 落码: `zeroImaginaryParts` (零点虚部集 def) + `zeta_zero_on_critical_line_iff` (成员判定) + `re_of_critical_line_point` (Re(1/2+iγ)=1/2, 谱实⟹临界线的代数原子) + `HilbertPolya` 结构体钉形状 (谱覆盖 + 谱实 → RH). **诚实: 核心未解 = 找到这样的自伴算子 H (外部输入); 零点虚部集⊆ℝ 是 trivial 的, HP 的真实内容是存在性; 这是 RH 的谱等价重述, 不给 RH 真值, 但把目标从零点分布转成谱结构 (与 leo 隐数/谱框架同构方向)**. 文档: docs/wiki/spectral-method.md |
| 2026-09-01 | feat | **§33 台阶1 实部提取桥落码完成 (Abundance.lean, 无 sorry, 3217 jobs 通过)**: `re_cpow_neg` (核心原子: Re((n+1)^{−s}) = (n+1)^{−σ}·cos(−s.im·log(n+1)), 经 eta_cpow_eq_exp + Complex.exp_re) + `re_cpow_neg_mul_I` (特例: Re((n+1)^{−it}) = cos(t·log(n+1))) + `mertens_three_four_one_re_nonneg` (3-4-1 实部非负真版本: Re(3+4(n+1)^{−it}+(n+1)^{−2it}) ≥ 0, 把 §32 的逐项非负从 cos 版本接到 ζ'/ζ 级数实部版本). **这完成了「数条数」非负性的代数链: Λ 加权逐项非负 (§32) + 实部提取 (§33) → 3-4-1 整体非负的 ζ'/ζ 表述就绪**. 编译踩坑 (cast 地狱, 4 轮): ①`↑(n+1)` vs `↑n+1` 不 definitional equal (Nat.cast 递归不 reduce), rw/exact 的 syntactic match 都失败 — 解法: 底数统一 `((n+1:ℕ):ℂ)` 原子 cast (匹配 eta_cpow_eq_exp), rpow/log 用 `(n+1:ℝ)` 直接形式, 并在 rw [eta] 后 `simp only [Nat.cast_add, Nat.cast_one]` 统一; ②rpow_def_of_pos 给 `log·y` 形式 (log 在前), 需 `rw [mul_comm]` 对齐; ③OfNat 的 `(3:ℂ).re` 不被 Complex.ofReal_re 处理 — 改陈述用 `((3:ℝ):ℂ)` ofReal 形式 + 精确 rw; ④`norm_num` 会展开变量 cast, 避免在底数处用 |
| 2026-09-01 | feat | **§32 台阶1 (ζ(1+it)≠0) 的 3-4-1 数条数骨架落码 (Abundance.lean, 无 sorry, lake build 3217 jobs 通过)**: leo 选 B(数条数逼近排除墙). 侦察确认 mathlib 齐备: `LSeries_vonMangoldt_eq_deriv_riemannZeta_div` (ζ'/ζ = −Λ 级数, Re>1, LSeries/Dirichlet.lean) + `ArithmeticFunction.vonMangoldt_nonneg` (Λ≥0) + `tendsto_riemannZeta_sub_one_div` (ζ 在 s=1 单极点, ZetaAsymp.lean). 落码: ①`mertens_lambda_term_nonneg` (真定理: Λ(n+1)·(n+1)^{−σ}·(3+4cosθ+cos2θ) ≥ 0, 逐项非负 = 数素数幂贡献); ②`mertens_lambda_sum_nonneg` (有限和非负, Finset.sum_nonneg); ③`zeta_ne_zero_on_one_line` def (台阶1结论: ∀t≠0, ζ(1+it)≠0); ④`OneLineZeroFree` 结构体钉形状 (term_nonneg/log_deriv_dirichlet 已证 + pole_log_deriv/zero_log_deriv 外部输入[零点重数 logDeriv 行为, mathlib 无] → conclusion). **诚实: 逐项非负与有限和是真定理; 完整 ζ(1+it)≠0 仍缺 ζ'/ζ 的 Dirichlet 级数实部提取 + 零点重数 logDeriv 行为 + σ→1⁺ 极限传递 (分析难点, 未落码). 坑: (n+1:ℝ) 解析为 ↑n+1 非 ↑(n+1) (老坑 §25 已记), rpow 非负用 add_nonneg (Nat.cast_nonneg n) zero_le_one; ∑ in binder 需 BigOperators, 改显式 Finset.sum |
| 2026-09-01 | research | **「数条数」路线推导 + 数值验证 + mathlib 侦察 — 复 Stirling 墙可降为整数 Stirling + 计数 (未落码)**: leo 提议用「数条数」(计数/流率) 换基座, 替换 ‖Γ(s)‖ 的 Stirling t-渐近这堵最后墙. 推导: 从 Gauss 乘积 Γ(s)=lim n!n^s/∏_{k=0}^n(s+k) (mathlib ✅ `Complex.GammaSeq_tendsto_Gamma`, Gamma/Beta.lean) 取模取 log, 在 k≈t 处劈开乘积: pieceA = σ·log T + log(T!) − (T+1)·log t → (σ−1/2)·log t + (1/2)·log(2π) − t (整数 Stirling, mathlib ✅ `factorial_isEquivalent_stirling` + `le_factorial_stirling`, Stirling.lean); 两个 Riemann 和 (k≤t 与 k>t) 各含 arctan→π/2 (圆周闭合), 合起来 −πt/2 + t, 与 pieceA 的 −t 抵消. 结果 |Γ(σ+it)| = √(2π)·t^{σ−1/2}·e^{−πt/2}·(1+O(1/t)). 数值验证 (mpmath, t=10..5000, σ=0.3/0.5/0.7): 误差→0 (σ=1/2 精确为 0, 因反射公式 |Γ(1/2+it)|²=π/cosh(πt) 精确), 误差 O(1/t²); Gauss 乘积 n→∞ 收敛确认; 分解三块 (pieceA/term2/term4) 逐一对上. **π 来自 arctan=圆周闭合, t^{σ−1/2} 来自纯计数簿记 (σ 数因子偏移) — leo「每弧度角动量/圆周=波长链闭合」的直接数学回响. 诚实边界: ①替换的是复 Stirling (鞍点/最速下降), 仍用整数 Stirling (Wallis, mathlib 已有) — 「无 Stirling」应精确为「无复 Stirling」; ②剩余新分析工作量 = Riemann 和的 O(1/t) 误差控制 (欧拉-麦克劳林一阶/有界变差, 初等但琐, 未落码); ③这只拆掉 Γ t-渐近这堵墙, 不拆排除墙 — 模平衡在零点处 0=0 退化, 即使拿到锐 |χ|~t^{σ−1/2}, 排除仍需无零点区域/零密度 (mathlib ❌). 未落码 |
| 2026-09-01 | feat | **§30 全局 ⟺: RiemannHypothesis ⟺ zeroFreeHalfPlane 落码 (Abundance.lean, 无 sorry, 编译通过)**: 把内禀排除判据与 mathlib 正式 RH 声明连成**双向等价**. 方向 1 `riemannHypothesis_implies_zeroFreeHalfPlane` (σ≥1 用 mathlib `riemannZeta_ne_zero_of_one_le_re` 含 s=1 junk value + 1/2<σ<1 用 RH 本身); sin 版函数方程 `zeta_eq_sin_mul_zeta_one_sub` (从 riemannZeta_one_sub 对 1−s 取, cos(π/2−x)=sin x, 无 Stirling); 纯算术 `two_mul_int_neg_eq_neg_two_mul_nat` (负偶整数=平凡零点形态); **`zeta_ne_zero_of_nonpos_re_of_nontrivial`** (σ≤0 排除: sin_eq_zero_iff + Γ_ne_zero_of_re_pos + riemannZeta_ne_zero_of_one_le_re + s=0 用 riemannZeta_zero, 全程特殊值分类无 Stirling); 方向 2 `zeroFreeHalfPlane_implies_riemannHypothesis` (σ>1 mathlib + σ≤0 排除 + 0<σ<1/2 反射配对 + 1/2<σ<1 排除本身); **`riemannHypothesis_iff_zeroFreeHalfPlane`** (组装). **意义: 内禀排除判据 (σ>1/2 无零点) 已严格等价于正式 RH — 组合 B 彻底闭环. 关键收获: 全局 ⟺ 的两个方向全程**无 Stirling**, 用的是函数方程 + sin/Γ 零点分类 + σ≥1 无零点 (PNT 前提, mathlib 已有) — 证明「排除 ⟺ RH」不需要 Stirling, Stirling 只在「证明排除本身」时才需要 (等价 Lindelöf)**. 编译踩坑: ①`(n+1:ℝ)` 加法 vs `((n+1:ℕ):ℝ)` 原子 cast 句法差异致 rw 失配 (§25 已记); ②`Complex.sin_eq_zero_iff` 给 θ=k*π, field_simp+ring_nf 后 s=2k 需 `rw [mul_comm]` 对齐 2*k; ③ℤ→ℂ 转换 norm_cast 行为难控, 改用 ℂ 层结论 + `simpa [Nat.cast_add, mul_add] using hn`; ④`Int.toNat (-k-1)` + omega 构造平凡零点 witness; ⑤`riemannZeta_ne_zero_of_one_le_re` 需 import Mathlib.NumberTheory.LSeries.Nonvanishing |
| 2026-09-01 | feat | **§28 组合 B: 内禀排除判据落码 (Abundance.lean, 无 sorry, 编译通过)**: 把「排除」钉到模平衡/反射这条内禀载体上. 交付: ①`zetaChi` (χ 传输因子, 抽自 §23 内联表达式) + `zeta_mod_balance_chi` (模平衡 χ 形态 \|ζ(1−s)\|=\|χ(s)\|·\|ζ(s)\|); ②`zeroFreeHalfPlane` (排除的经典形态 ∀σ>1/2: ζ≠0); ③**严格 ⟺ `zeta_zero_iff_pair_both_zero`** (零点 ⟺ 反射对**同时消失**, 组合 zeta_mod_balance, 无 Stirling); ④**严格 ⟺ `zeta_norm_ne_zero_iff_pair`** (非零 ⟺ 反射对**同时非零**, 组合 §16 zeta_zero_iff_one_sub 的非零版, 无 Stirling); ⑤结构体 `GammaImbalanceReduction` (收拢 §20/22/23 三线: mod_balance/chi_decomp/pair_both_zero/pair_ne_zero/exponent_coord 已证 + chi_asymptotic 显式 Stirling 输入 → zero_free_half_plane). **反思 (做完后的诚实评估)**: 两条严格 ⟺ 是「反射配对」的零/非零两版包装, 不携带排除新强度 (符合信息守恒预期); 组合 B 的真价值 = 用 Lean 钉死「排除 = 反射对整体非零」这个内禀判据, 并把唯一定量缺口精确到可独立计算的 χ 渐近 (等价 Lindelöf). 未做 (收尾): mathlib `RiemannHypothesis ⟺ zeroFreeHalfPlane` 的全局 ⟺ (需处理 σ>1/σ<0/s=1 边界 case) — 已于同日 §30 完成 |
| 2026-09-01 | feat | **§25 (bounded_variation 落码) — η 条件收敛从「外部输入」变成「定理」 (Abundance.lean, 无 sorry, 编译通过)**: 完成 §24 AbelGate 的 `bounded_variation`/`tends_to_zero` 两个「外部输入」的真证明 + η 条件收敛最终组装. 核心链: ①`complex_exp_sub_one_norm_le_two` (全局 ‖exp w−1‖ ≤ 2‖w‖·exp‖w‖, 分 ‖w‖≤1/≥1 两段, 无 Stirling); ②`eta_cpow_eq_exp` ((n+1)^{−s}=exp(−s·log(n+1)), 底正实无分支); ③`eta_variation_step` (精确步: ‖(n+1)^{−s}−(n+2)^{−s}‖ ≤ (n+2)^{−σ}·2‖s‖·log((n+2)/(n+1))·exp(·), 差分=exp(z')·(exp(z−z')−1) 拆解); ④简化引理 log((n+2)/(n+1))≤1/(n+1)、≤log 2、(n+2)^{−σ}≤(n+1)^{−σ}; ⑤**`eta_bounded_variation`** (Σ‖(n+1)^{−s}−(n+2)^{−s}‖ 可和, σ>0, 对比 p 级数 Σ(n+1)^{−σ−1}); ⑥`eta_tends_to_zero` (b_n→0, tendsto_rpow_neg_atTop); ⑦**`eta_partial_sums_converge`** (η 部分和 ∃ 极限 — 条件收敛正确形态, 非 Summable 因 ℂ 上 Summable⟺绝对可和; Cauchy 判定 = alternating_sum_bound 尾估计 + 变差可和 + 项趋于零 + ℂ 完备). **诚实: 不给 RH 加任何排除信息, 是 bookkeeping — 让 §21 对齐语言在临界带良定**; 剩余 Hadamard 链 [B] Γ 上界/[C][E]/零密度仍独立. 编译踩坑: ①`(n+1:ℝ)` 解析为 ℝ 加法 `↑n+1` 而 `((n+1:ℕ):ℝ)` 是原子 cast `↑(n+1)`, 句法不同致 rw 失配 — 全程统一原子 `((n+k:ℕ):ℝ)` 形式; ②`in` binder 需 `open scoped BigOperators` 且与 `∈ Finset.` 版句法差异, 改显式 `Finset.sum`; ③`sum_le_sum_of_subset_of_nonneg` 的 hf 是 `∀ i∈t, i∉s → 0≤f i` (两参数); ④`Metric.cauchySeq_iff`(双边) vs `cauchySeq_iff'`(单边 u n vs u N) 勿混; ⑤`Metric.tendsto_atTop`(ε-δ) 而非 tendsto_nhds |
| 2026-09-01 | feat | **§24 (A-gate) 复值 Abel 分部求和 — 排除性论证的入口闸门落码 (Abundance.lean, 无 sorry, `lake build RiemannHIBS.Abundance` 通过)**: abelPartialSum + abelPartialSum_succ + **abel_sum_by_parts** (Σ_{n≤M}a_n b_n = A_M b_M + Σ_{n<M}A_n(b_n−b_{n+1}), 归纳+ring, 纯代数) + alternating_abel_twice (A_n=(1−(−1)^{n+1})/2) + **alternating_abel_norm_le_one** (交错部分和模 ≤1) + **alternating_sum_bound** (Dirichlet 主估计: ‖Σ(−1)^n b_n‖ ≤ ‖b_M‖ + Σ‖b_n−b_{n+1}‖, 与 M 无关 ⟹ 直接给 Cauchy 尾估计) + AbelGate 结构体 (已证工具 3 条 + 显式声明 bounded_variation/tends_to_zero 两个外部输入). **地位: [A] 是 Hadamard 链 (条带 ζ 上界) 与 §21 η 临界带桥的共同钥匙**, 无它任何排除性估计进不了场. **诚实: 闸门≠终点 — 走完 Hadamard 链只复现 Hadamard 1893, 不证 RH**; 剩余 [B] Γ 上界 (初等推导已给: \|Γ(σ+it)\| ≤ Γ(σ+n)/\|t\|^n ≤ 2^{\|t\|}\|t\|^σ, 拆分积分+指数尾+递推, 无 Stirling) / [C][E] / 零密度 仍为独立工作量. 编译踩坑记录: `rw [Finset.sum_range_succ]` 会命中 LHS 内层和新展开的和 (遍历序先左后右), 须用 calc+`conv_rhs` 定向; `mul_le_mul_right` 在本环境非 iff 形态, 改用 nlinarith; `add_le_add_left` 生成右加形式, 改用 `add_le_add (le_refl _) h` |
| 2026-09-01 | fix | **论文推论 6 (cor:rh-reduction) 逐句推导复核 (外部质疑触发; 按推导判定, 不按日志判定)**: 原推论两句**真值不同** — 第2句「不存在偏离 ‖w‖=√e 的非自配对零点」**正确且 ⟺ RH**(用的是 `self_pair_on_circle` 的有效**逆否** ‖w‖≠√e ⟹ e/w≠w, 故"偏离圆"已自动蕴含"非自配对"); 第1句「每个反射对都自配对」**有歧义, 按点层定义则为假**. 关键 = 区分反演的**集合层固定** (‖e/w‖=‖w‖ ⟺ ‖w‖=√e, 圆送到自身) 与**点层固定** (e/w=w ⟺ w=±√e, 提升高度 t∈πℤ). 在 ‖w‖=√e 上反演=共轭 (e/w=conj w), 故 RH 下零点是**共轭对二元轨道**而非不动点. 推导: 圆上 e/w=conj w ⟹ e/w=w ⟺ e^{2i·Im s}=1 ⟺ **Im s∈πℤ**; 反例 γ₁=14.13472514 严格介于 4π≈12.5664 与 5π≈15.7080 之间 ⟹ ∉πℤ (单个反例足够); 渐近二次否决 (πℤ 间距 π vs 平均零点间距 2π/log(T/2π)→0, T=1000: ≈807 个零点 vs 319 个 πℤ 点); 覆盖点层更严 (1−s=s ⟹ s=1/2, 而 ζ(1/2)≈−1.4604≠0) ⟹「补上逆命题」是数学死路, 非猜测; 该说法严格强于 RH 且与已知零点冲突. 已改: ①推论重写为「每个反射对整体落在临界圆 ‖w‖=√e 上 ⟺ 圆环内无 ‖w‖≠√e 的零点」; ②thm:selfpair 补逆命题不成立的说明 (e/w=conj w, 仅 t∈πℤ 时重合); ③中英摘要/开放命题清单/推导边界第 4 步同步订正; ④Abundance.lean §16 `InversionPairDecomposition` 字段 `no_nonself_pair` 改名 `no_zero_off_circle` — 注: 蕴含方向 (自配对 ⟹ ‖w‖=√e ⟹ RH) 本身为真, 错的是"等价"; 且点层读法是**假命题**, 假命题不可证, 不能充当缺口. Lean 定理 `self_pair_iff`/`self_pair_on_circle` 单向正确, 未受影响. 双语 tectonic 重编译通过, `lake build RiemannHIBS.Abundance` 通过 |
| 2026-08-26 | feat | R6 频率结构 + §19 完整论证 (Abundance §18-19, 无 sorry): freq_gap_tendsto_zero (Δfreq→0) + freq_gap_antitone (间距单调递减) + phase_freeze (相位冻结: 高频项相位差任意小) + fold_point_exists (相位折返点 n*≈t/π: Δθ=π−t·Δfreq 单调从负到正, Nat.find 最小正点) — 零点高度在频率域的回声; CriticalCircleInfinity 组装: 位置 (R2+R4) + 频率 (R6) + 能量 (R1+B3 均值定理无 Stirling) + 密度 (R5 N/F→1) + Hardy 反证 ⟹ 临界圆无限零点 (有位置+有丰度), 每环引用已证定理或显式外部输入 |
| 2026-08-26 | feat | R5 对齐密度猜想 (Abundance §17 + experiments/r5_alignment_density.py + viz/fig15): FrequencyAlignmentConjecture 显式声明 — 零点密度 = 频率相位投影 F(T)=(T/2π)(log(T/2π)−1) 无 Stirling; 数值 N(T)/F(T)→1 (T=100..3200: 1.0310→1.0003) + δ̄=1.0000; 反演对分解数值对照 (平凡零点 −2k 反演对 1+2k 处 ζ≠0 被排斥因子破坏 vs 临界带零点成对 |ζ(1−s)|≈0) — 对分解只属临界带; R5c 诚实发现: 临界带部分和 |S_N| 不趋于 0 (γ₁ 处 0.25→1.42) — 对齐是解析延拓零点非级数零点, §10 对齐条件 Re>1 版不能直接搬临界带; 严格化需等分布 (mathlib 无) |
| 2026-08-26 | feat | R4 反演对分解 (Abundance §16, 无 sorry): zeta_zero_iff_one_sub (临界带反射零点等价 ζ(1−s)=0⟺ζ(s)=0, riemannZeta_one_sub 函数方程 + 因子非零: Γ 经 Gamma_ne_zero_of_re_pos, cos(πs/2) 经实部 Re cos = cos(πσ/2)cosh(πt/2)>0, cpow_ne_zero_iff) + zero_pair_reflect (零点成对) + self_pair_iff (自配对 w=e/w⟺w=±√e 因式分解) + self_pair_on_circle + reflect_involutive; 命题甲压缩为"无非自配对反演对"; InversionPairDecomposition 声明 (缺口 = RH 核心) |
| 2026-08-26 | feat | R3 对齐模长机制 (Abundance §15, 无 sorry): 圆外远处无零点的纯内禀证明 — σ≥2 时 Σ_{n≥1}(n+1)^{-σ} ≤ 3/4 (逐项 1/(n+2)² ≤ 1/((n+1)(n+3)) + 望远镜) ⟹ 部分和 ‖S_N‖ ≥ 1/4 (norm_add_le 反向三角 + norm_sum_le) ⟹ ‖ζ(s)‖ ≥ 1/4 ⟹ zeta_ne_zero_of_two_le_re (2 ≤ Re s ⟹ ζ(s)≠0, zeta_eq_tsum + ge_of_tendsto); 机制与欧拉乘积正交 (对齐模长, 无解析延拓依赖); alternating_harmonic_limit_pos (η 实轴正性 l≥1/2, Leibniz 夹逼); IntrinsicZeroFree 诚实标注 (σ∈(1/2,1) 圆环 = RH 核心无内禀通道) |
| 2026-08-26 | feat | C 对齐⟹零点临界带骨架 (Abundance §14, 无 sorry): alternating_harmonic_converges (交错调和收敛, Leibniz — Antitone.tendsto_alternating_series_of_tendsto_zero) + leibniz_even_le_limit / leibniz_limit_le_odd / leibniz_error_quantified (偶部分和≤极限≤奇部分和, Leibniz 误差夹逼); AlignmentToCriticalBand 结构体 (外部输入: η 复侧条件收敛 + Abel 延拓 + 排斥因子 (1−2^{1−s}) 临界带恒非零 — 显式声明) |
| 2026-08-26 | feat | R1 交叉上界全链 Lean 化 (Abundance §12, 无 sorry, 提交 53660e8/2d3ee07/dd09bb6): sqrt_div_le + cross_row_bound (行界 Σ_{m<n} √(n/m)/(n−m) ≤ √n(1+log(n−1)), sum_range_reflect + harmonic_le_one_add_log) + cross_total_bound (≤ N√N(1+log N)); 截断 X=√(T/2π) 交叉 ~T^{0.75}·logT 次主导于对角 T·logT (数值 0.27→0.05); sum_re + integral_re_comm + partialSum_sq_pointwise (|Σv|²=对角+交叉) + diagonal_energy_half (∫₀ᵀ‖v_n‖²=T(n+1)⁻¹) + TruncatedEnergyLower 结构体 |
| 2026-08-26 | feat | R2 半径唯一性骨架 (Abundance §13, 无 sorry): inversion_fixed_radius (反演不动 |e/w|=|w| ⟺ |w|=√e, 纯代数, Complex.ofReal_exp+norm_exp) + radius_uniqueness_chain (三线合一: 反演不动 ⟺ energy_balance_radius ⟺ 反射不动) + ZeroRadiusUniqueness 声明 (唯一候选+机制等价已证; 缺口 zeros_on_candidate = 对齐⟹半径锁定 = RH 本身, 诚实标注; 机制环 mechanism_cycle_closed 已证故缺口形态精确 = 补 hno: 零点⟹Re s≤1/2) |
| 2026-08-26 | feat | 论文输出: 依据 wiki 真实成果生成双语论文（英文版 riemann_hibs_en.tex 1026 行 + 中文同步版 riemann_hibs_zh.tex），基于 jsfds 模板，含 11 节 57 个编号定理环境，覆盖隐数坐标 w=e^s 全框架（临界圆、反演不动圆 e→√e→1/2、反射-反演对应、零点定位圆环 1<|w|<e、相位机制、幅角原理/幂因子绕数、证书族与高度供给、能量发散/Hardy 方向、诚实边界完整列表），严格区分已证结构定理与开放分析输入；作者 Yuanjie Liu, yuanjieliu65@gmail.com；biblio.bib 追加 9 经典条目
| 2026-08-25 | init | 初始化知识系统: bootstrap v2 (LLM Wiki 范式), 8 页面 + manifests + 校验脚本 |
| 2026-08-25 | research | 包络结构: 临界线卷成圆 ‖w‖=√e (criticalLine_circle), expZeta, 桥定理, 多圈叶/折叠, Möbius 随机性; 数值验证 E1-E4 + 10 图 + 螺旋动画 |
| 2026-08-25 | research | 半径叶与缠绕: log(√e)=1/2, 临界叶上每项模长锁定 n^{-1/2} (长度维度锁定), 缠绕圈数 log(n+1), 绝对收敛分界 r>e, 临界叶不绝对收敛 (radius_leaf 方向, Analytic.lean §11) |
| 2026-08-25 | feat | 目标达成: ZetaCoverIsomorphism 结构 (覆盖↔复平面同构, 全由已证定理构成, 无 sorry) + helix_continuation 螺旋延续 (§12); 机制完备环 A⟹B⟹C⟹D⟹A 四方向全证 (§13, A 圆外无零点 / B 零点在圆上 / C 包络半径锁定 ‖e^s‖=√e / D 经典 RH, 输入 h0: 0<Re s) |
| 2026-08-25 | research | 无限多个零点 (§15): 平凡零点无限多已证 (infinitely_many_trivial_zeros, 单射嵌入 n↦−2(n+1)); 非平凡部分如实标注边界 (Hadamard 1893 需增长阶+因子分解/幅角原理, 已有机理不携带 ζ 增长行为) |
| 2026-08-25 | research | 零点分布规律 (§14): 修正两跳跃 — 圆外远处无零点已证 (Re≥1 欧拉乘积, mathlib), 负侧无零点 (函数方程反射), 非平凡零点 ⟹ 0<Re<1 ⟹ 圆环 1<‖e^s‖<e 内; 临界叶√e 是圆环几何平均正中 |
| 2026-08-25 | feat | 隐数坐标系幅角原理 (§16, 局部版): argumentPrinciple_single_zero (∮dz/(z−w)=2πi 单零点原子) + envelope_circle_param (圆参数化) + phase_winding_equals_zero_count (相位缠绕=零点数) + zeroCount_normalized ((2πi)⁻¹∮=1); 诚实标注: 幅角原理为"电路"缺"电源", 无限零点仍赖增长估计输入 |
| 2026-08-25 | research | 无限幅角原理的增长分解 (§17): zeta_growth_decomposition (ζ=compZ/Γℝ) + gammaR_norm_decomposition (\|Γℝ\|=π^(−Re s/2)·\|Γ(s/2)\|) + gammaR_norm_in_envelope_coords / zeta_norm_in_envelope_coords (隐数坐标精确读法, 纯代数已证); 预言 P1–P3 (Stirling + 完成ζ有界) 明确标注为 mathlib 缺失, 把"增长估计"隔离为唯一待形式化边界 |
| 2026-08-25 | research | 有限相位抵消的组合翻译 (§18): 用户思路 — 不证无限性, 转成"有限个自然数相位能否被有限周期度数抵消". phaseCancelRelation (Σaᵢ·log(nᵢ)∈2πℤ) + phaseCancel_of_exp_one (⟹ exp(i·Σ)=1) + phaseCancel_zero_iff_powProduct_one (抵消回0度 ⟺ Π(n+1)^{aᵢ}=1 ⟺ 整数乘法结构); 路线C框架 periodicCancelWindow (360°周期窗口); 路线B稠密性依赖 log ℚ-无关/超越性, mathlib 未形式化, 如实标注阻塞 |
| 2026-08-25 | direction | 候选讨论: "有限组合的 360° 相位抵消" 作为零点构造入口 — 有限前缀 S_N 与尾误差 R_N 精确配平 ⟹ 零点; 已证支撑 (dirichlet_term_rotating_vector / critical_leaf_norm_locked / zeta_phase_alignment_condition), 诚实边界= 单靠有限 S_N=0 不等价 ζ=0, 需 |R_N| 随 N 无界受控(即 §16/§17 增长输入); 有限相消把无限性沉进尾误差而非消掉, 无限版仍赖增长估计; 记为候选机制待推进 |
| 2026-08-25 | research | 缺口① 笔算推导 (不落码, 先想清): 关键翻转 — `dirichlet_term_rotating_vector` 是复恒等式(与 σ 无关), 故相位/旋转向量机制对任意 Re 成立; 临界带断路在"级数=ζ"而非相位. 正解: 边界带 `ζ(s)=0` = 全纯函数取零(mathlib differentiableAt) — 不需级数和; 隐数语义"对齐零点"=覆盖空间 `expZeta_phase_principal` (ζ̂(w)=ζ(log r+iθ)) 良定等式. 经典桥: Re>0 η 交替 Abel 条件收敛 + 排斥因子 (1−2^{1−s}) 在临界带恒非零 (2^{1−s}=1 仅实部=1, 不在带内) ⟹ ζ=0⟺η=0 在临界带成立. 结论: 缺口① 的真阻碍 = mathlib 缺"条件收敛(η)+Abel/函数方程延拓"工具, 非概念不可能; 建议走覆盖语义"对齐零点"命题 + 临界带诚实标注. 缺口② 保持如实(N(T)→∞ 缺增长输入) |
| 2026-08-25 | verify | 缺口①/② 审计 (§10→§14→§17 连通性): 验证确认构建通过 (Analytic.lean 无 sorry, 仅 ring linter 建议); 缺口① = `zeta_phase_alignment_condition` 限 Re>1 (级数直接=ζ), 临界带 0<Re<1 内 ζ 为解析延拓, "旋转向量和=0" 与 "ζ=0" 无已证桥 (需 η/函数方程/Abel 正规化延拓); 缺口② = "N(T)→∞" 依赖 Stirling+完成ζ有界 (P1–P3, mathlib 缺失), 即使完成也只证"无限多"非 RH; 两缺口已写入 current-status 诚实边界 |
| 2026-08-25 | feat | §19 缺口① 桥落地 (代码, 无 sorry 编译通过): eta_term_rotating_vector (η 第 n 项=(−1)^n×旋转向量, 复恒等式, dirichlet_term_rotating_vector 反向 rw+ring) + eta_phase_alignment_condition (Re>1 下 η(s)=0 ⟺ 交错旋转向量和=0, 镜像 §10 换交替符号); 把"相位对齐/抵消"语言与 η 级数接通; 临界带 0<Re<1 的严格对齐需条件收敛 η+Abel/函数方程正则化, mathlib 缺此基建, 如实标注为待形式化 (不装证) |
| 2026-08-25 | feat | §20 隐数对齐零点落地 (代码, 无 sorry 编译通过): alignmentZero (零点的"相位对齐"信息压缩谓词 — s 对齐零点 ⟺ 交错旋转向量(η 形态)和=0) + alignmentZero_iff_zeta_zero (Re>1 桥, 前提排斥因子≠0: alignmentZero s ⟺ riemannZeta s=0, 经 eta_eq_mul_zeta); 把既有零点重述为相位结构显式化, 非发明独立对象. 诚实标注: 无限非平凡零点仍是独立边界(需条件收敛 η+Abel/函数方程(§19) + 增长输入(§16/17)), 未装证 |
| 2026-08-25 | direction | 无限非平凡零点 目标精确化 (分析整理, 不写伪证): 用户主张 "在隐数空间构造了所有非平凡零点的可能相位 (§18/19/20 alignmentZero), 接下来统一证明增长模式使有界而无限". 关键修正: (i) §20 alignmentZero 是"判定对齐的谓词", 覆盖所有 s 的相位, 但不保证任何 s 存在 — 非平凡零点无限缺"存在性", 而平凡零点无限(§15)靠显式公式 ζ(−2(n+1))=0 无需分析; (ii) "逐项模长锁定 n^{−1/2}"(§11)≠"完成ζ整体有界 P2"(§17 预言未证), 后者才是幅角/零点计数输入; (iii) "自然数增长无限"(项指标)≠"零点集无限"(复参数集) — 两个不同概念, 不能由前者直接推后者. 目标命题 G 已精确化写入 current-status; 螺旋增长模式可先定义为对象(spiralGrowth)作攻坚入口, 但增长界本身未证, 如实标注 |
| 2026-08-25 | direction | 隐数驱动存在性候选路线: 在万有覆盖取有限高度螺旋矩形/圆环扇区 Ω_T, 证明边界无零点且提升函数边界像绕数非零, 再由幅角原理推出至少一个零点; 无界序列 T_j 或 N(T)→∞ 才给无限性. 明确边界: 有限相位抵消与逐项模长锁定不控制级数尾项, 且不同覆盖叶上的 ζ(s+2πi) 不周期重复, 因而不能单独推出零点存在或重复. 隐数提供的是可验证的围道/对称/相位组织, 不是凭坐标变换创造零点 |
| 2026-08-25 | feat | §14.5 存在性转换器落地并通过构建: `inversion_preserves_annulus`; `AnnularNontrivialZero` 与 `annularNontrivialZero_of_zero`; `reflection_involutive`; `annularNontrivialZero_reflects` / `_iff_reflects`; `on_circle_reflects_on_circle`; `exists_annular_nontrivial_zero_pair`. 明确语义: 反射给出圆环零点的二元轨道和临界叶集合保持，不提供第一个非平凡零点，也不提供无限性；平凡负偶零点不满足反射定理的负整数排除前提，不能被错误地用作圆环种子 |
| 2026-08-25 | feat | §14.5 覆盖高度接口落地并通过构建: `hiddenStripWindow` 定义有限高度提升矩形; `hiddenNontrivialZeroHeights` 与 `HiddenZeroHeightGrowth` 把螺旋角度 θ=Im(s) 的无界性精确定义; `infinite_annular_nontrivial_zeros_of_height_growth` / `infinite_nontrivial_zeros_of_hidden_height_growth` 证明高度无界蕴含圆环/经典临界带中的无限非平凡零点. 诚实边界: 高度无界本身仍需完成 ζ 的围道绕数或增长估计，未装证 |
| 2026-08-25 | feat | §14.5 矩形围道原子落地并通过构建: `hiddenLiftRectangle`、`hiddenRectangleBoundaryIntegral` 及 `hiddenRectangleBoundaryIntegral_eq_zero_of_differentiableOn`; 接入 mathlib 的 `Complex.integral_boundary_rect_eq_zero_of_differentiableOn`, 将有限高度螺旋矩形纳入严格围道积分框架. 诚实边界: 该原子只处理全纯函数边界积分为零, 尚未给出 `logDeriv` 零点计数或完成 ζ 的增长下界 |
| 2026-08-25 | feat | §21–§22 存在性与坐标传递接口落地: 修正 `analyticAt_riemannZeta` 的去心邻域证明；完成无零点时 `logDeriv` 圆周积分为零、积分非零⟹闭圆盘有零点、`AnnulusZeroWitness` / `CriticalStripWitness` 及 `ZeroHeightSupply` 全链传递。`lake build RiemannHIBS.Analytic` 通过；无条件的经典高度供给仍明确标为待证明输入，未使用 `axiom` 或 `sorry` |
| 2026-08-25 | feat | §23–§24 幅角原理核心原子与单零点证书落地 (无 sorry 编译通过): ① `powFactor_logDeriv_eq` 用 mathlib logDeriv_mul/pow/comp 组合路线证明幂因子 log 导数逐点恒等式 (规避 nat 减法幂代数); ② `winding_of_pow_factorization`: f=(z−w)^m·g 于闭盘 ⟹ ∮f'/f=2πi·m (积分线性拆分+§16 原子+23.1 一般版 Cauchy); ③ `ZetaSimpleZeroCertificate` 参数化结构 (w,g 显式参数使字段全为命题) + deriv 局部相容桥 (`Filter.EventuallyEq.deriv_eq`) ⟹ 证书⟹∮ζ'/ζ=2πi⟹AnnulusZeroWitness⟹反射对全链接通. 诚实标注: Hardy 数值路线因 mathlib 缺区间算术阻塞, 证书本身仍是分析输入未提供实例 |
| 2026-08-25 | feat | §25–§26 三阶段第二/三步闭环与零点隔离工具落地 (无 sorry 编译通过): ① `UnboundedZetaCertificateFamily` 改名 `UnboundedZetaCertificateAssumption` 并加外部假设标注框 (def 形态条件谓词, 非 axiom); ② `zeroHeightSupply_of_certificateFamily` 证书族⟹高度供给主桥; ③ `classicalZeroCountGrowth_of_zeroHeightSupply` 高度供给⟹计数增长补桥 (Infinite.exists_subset_card_eq 取 N+1 互异点 + Finset.image max' 定窗 + encard 单调链); ④ §26 `no_zero_of_boundary_dominance` 无零点稳定性 (Complex.norm_le_of_forall_mem_frontier_norm_le 最大模原理 + DiffContOnCl 闭盘构造 + frontier_closedBall⊆sphere 条件传递); ⑤ `zeta_ne_zero_of_boundary_unit_bound` ζ 排除判据 (g≡1, h:=ζ−1). 新增 import Mathlib.Analysis.Complex.AbsMax; 诚实标注: Rouché 存在半边需 winding number 理论 mathlib 缺失 |
| 2026-08-25 | feat | §18.6 一个组合×无限角度 (用户思路落地, 无 sorry 编译通过): phaseShift_cancel_of_int + phaseShift_aligned_at_integer — 一个在 t=0 抵消的窗口 (phaseCancelSum=2πk) 在整数平移 t 下相位回到 2πℤ, 即"一种组合在整数倍角度反复对齐"的代数真身. 诚实边界: 这是**部分和窗口**的相位平移, 不证 ζ 零点 (窗口是有限项, 完整 ζ 含尾项); 是否对应零点仍需尾项/增长输入 (§22 ZeroHeightSupply/证书族), 如实标注 |
| 2026-08-25 | feat | §18.8 调制基元族落地 (无 sorry 编译通过): 用户主张"把组合当基元, 通过调制覆盖尾项/无限高度". 核对: "固定基元+调制t"已证(§18.6, 有限窗口相位性质, 覆盖不到无限尾项指标); "同基元调制覆盖无限高"的真载体是参数化证书族(§25 UnboundedZetaCertificateAssumption). 据此封装 ModulatedZetaFamily = 无界证书族, 并加两条桥定理 (⟹ZeroHeightSupply, ⟹无限非平凡零点). 诚实: 族的具体实例(每高度真给证书)仍是分析供给黑盒(§22), 未提供; 此处只给"若族存在则无限闭链打包"接口 |
| 2026-08-25 | direction | 评估用户"更简单思路"(圆上无限点⟹存在性): **不成立**. 把几何连续无限(圆周不可数点集, 圆公理)误当成函数零点的无限(ζ 取 0 的离散点). "圆上有无限点"≠"ζ 在圆上有无限零点"; 前者是集合无限, 后者是取零位置的存在性. "圆外无零点+圆环定位"只排除/定位, 不证明存在. 与已落地的唯一黑盒 `ZeroHeightSupply`(任意高度之上临界带零点) 对照: 圆连续无限不提供任何具体零点实例, 故不能补该输入. 已记入 current-status 诚实边界 |
| 2026-08-25 | direction | 评估"角度调制生长覆盖尾项" (用户主张): **不成立** — 角度 t 与指标/频率是两个独立自由度. 旋转项 (n+1)^{-σ}·exp(-i·t·log(n+1)) 随 t 只让已有项绕圈, 不产生新指标的新频率 log(n+1) 与新权重 (n+1)^{-σ}; 尾项需一批不同频率+权重的新项, t 调不出(摇音叉不产生第二支不同音高的音叉). "生长"要成立须在频率域(制造新 log(n) 项而非相位 t), 频率域生长=引入新组合=回到证书族(§25)/多组合. 已记入 current-status 候选机制(被否). 活路 = 证书族实例(§18.8/§25), 仍待分析供给 |
| 2026-08-25 | direction | 坐标内决定性退化甄别: "有限相位组合→无界高度"不成立 — 有限整数系数 log 组合=log(有理数), 入 2πℤ 仅 k=0 (e^{2πk}=有理数仅当 k=0, 超越性) ⟹ 所有 phaseCancelRelation 的组合 phaseCancelSum=0 (§18.3 退化). 故 §18.6 相位 -t·phaseCancelSum 恒0, 沿整数角 t 高度恒0无增长 — "基元对齐⟹螺旋上升无界"在定义内为假. 此否定在坐标系+自身定义内推出, 非经典框架. 无界高度只能来自非对齐单项转动+完整项/尾项, 回到存在性黑盒. 已记入 current-status 候选(被否) |
| 2026-08-26 | research | 坐标系 e 的独有规律 (用户深挖「零点为何与 e 有关」): 经典复平面临界线只是 Re s=1/2 凭空定义的线; 隐数 w=e^s 下该线身份被 e 唯一选定 — 函数方程反射 s↦1−s 翻译为反演 w↦e/w, 不动圆 = ‖w‖=√e (envelope_inversion_fixed_circle), 取 log 得 1/2 (log_sqrt_exp_one). 已证内核: 1/2 = log(√e) 是反演不动圆半径的必然对数值 (坐标系独有真洞察). 关键纠偏: 「位置」拆两层 — 层①圆的身份(已证, e 解释为何是 1/2); 层②零点在圆上(未证, 依赖 §13 hno 输入, 非 e 单独给出). envelope_inversion_eq_conj_on_circle (e/w=conj w) 是全圆几何恒等式, 只给条件句不自证存在. 丰度(无限) e 完全不涉及 — 线是连续一维对象, 推不出离散零点存在/无限; 唯一诚实无限接口仍是 HiddenZeroHeightGrowth/ZeroHeightSupply. 本方向属同类第三只已否跳跃 (圆上无限点/有限相位组合/自共轭圆 → 无限零点), 均把坐标变换几何误当 ζ 存在来源. 落档: current-status 新增「坐标系独有规律」小节 + 诚实边界补「e→1/2 已证的是圆的身份非零点在圆上」 |
| 2026-08-26 | proved | 隐数坐标「相位预算」形式化验证 (用户要求数学验证非只文档): 在 Analytic.lean §11.5 新增并 `lake build` 通过三条定理 — critical_leaf_phase_velocity_term_norm (单项相位速度模长=(n+1)^{−1/2}·log(n+1)), critical_leaf_phase_velocity_budget_increasing (预算随 N 单调累积), critical_leaf_phase_velocity_positive (N≥1 恒正). 验证了「隐数坐标能承载丰度指标」在代数/语法层非死路 — 方向 A (临界叶相位预算) 从语法层确认可行, 区别于已被否的「有限相位组合⟹无界」. 诚实边界: 只证预算累积, 不证「预算在某 θ 归零⟹零点」; 尾项 Σ_{n≥N}n^{−1/2}·(振荡) ~ N^{1/2} 不趋于0, 故临界圆上「预算累积⟹归零」不成立, 丰度仍须经 Stirling 分析估计 (§17 P1–P3 / §14.5 黑盒). 落档: current-status 新增「已验证: 隐数坐标的相位预算」小节 |
| 2026-08-26 | insight | 记录隐数坐标中的 e 规律: 反射 s↦1−s 在 w=e^s 下为反演 w↦e/w, 不动圆为 |w|=√e, 且 log(√e)=1/2; |w|=e 对应 Re(s)=1 的收敛分界. 该结构解释临界位置为何是 1/2, 但不供给零点存在性或无限丰度, 明确区分位置与丰度 |
| 2026-08-26 | insight | 新增 ξ 载体与能量发散清单: `xiEntire` 的整函数/对称/乘积基础已落地, 但 ξ 无限零点的 Hadamard 组合链及 ξ↔ζ 临界带桥仍需显式补证; 临界叶能量单项 `(n+1)^(-1)` 与调和级数发散已证, 均值公式和 Hardy 过零论证未证. 结论: log 积累是燃料, 不是从能量到零点的引擎 |
| 2026-08-26 | insight | 深化 e 规律三精确化: ① 圆上反演=共轭 (`envelope_inversion_eq_conj_on_circle`, §6 机制 e 已证) — 全部半径中唯一的"镜像=自共轭"圆, 临界线上零点反射对即共轭对的几何表达; ② 不动圆唯一性 — |w|²=e 正实数解唯一 √e, "为什么 1/2"升级为反射几何的结构必然 (非约定); ③ 丰度瓶颈精确化 — 连续自共轭圆不向其离散子集传递基数信息, "位置为何 1/2"与"丰度为何无限"正交, e 几何对丰度贡献为零. 落 current-status「e 的独有规律」小节 |
| 2026-08-26 | direction | 丰度指标盘点 (回应"隐数坐标是否无丰度指标"): 三个未利用种子 — ① 能量发散指标 (最具体): critical_leaf_norm_locked ⟹ L²能量 ~ log T 发散, 即 Hardy 1914 出发点, 位置+丰度合一且不需 Stirling, 缺均值公式 (mathlib 无); ② 频率 Diophantine: mathlib 已有 Lindemann 解析部分, §18.5 阻塞缩至代数半边; ③ R-vM 公式的 2π 坐标读法: T/2π 即覆盖角归一化, 零点密度 = 角度密度 × 对数尺度熵 (视角贡献). 新候选机制落 current-status |
| 2026-08-26 | feat | §27 ξ 整函数载体落地 (无 sorry 编译通过): 核心发现 = mathlib completedRiemannZeta₀ 是去极点辅助整函数 (Λ₀=Λ+1/s+1/(1−s)), 其零点与 ζ 无关, 但代数变形 ξ(s)=(1/2)s(s−1)Λ₀(s)+1/2 把经典 ξ 的整性/对称性变成 Λ₀ 性质的多项式推论 (无需 removable singularity); 落地 xiEntire/differentiable_xiEntire/xiEntire_one_sub/xiEntire_eq_mul_zeta/xiEntire_eq_zero_iff_zeta_zero (临界带 ξ=0⟺ζ=0, Γℝ≠0 经 Gamma_ne_zero_of_re_pos + cpow_ne_zero_iff_of_exponent_ne_zero); §28 Hadamard 组合链设计标注 (Borel–Carathéodory→G常数→对称强制多项式→实轴矛盾, Γ粗上界+递推下界全初等, 不需 Stirling); 另修复并行会话 Hardy 能量代码两处编译错误 (normSq_ofReal 正确名 / summable_nat_rpow 的 p<−1 形态) |
| 2026-08-26 | feat | §29 Hadamard 链组件 [G] 落地 (无 sorry): `Real.gamma_ge_pow_mul_self` — Γ(x+n) ≥ xⁿ·Γ(x) 对 x≥1, Real.Gamma_add_one 归纳迭代 + mul_le_mul 单调链接, 完全初等无 Stirling; 同节登记 Borel–Carathéodory 依赖图全状态 ([D]BC定理 mathlib 已有 Complex.borelCaratheodory, [G]本轮落地, [A][B][E] 中段未形式化); 明确 B-C 链补 Hadamard 侧反证组合、不补 Hardy 均值公式缺口 |
| 2026-08-25 | research | 频率丰度机制: 能量=调和发散, 临界线=能量临界点; Abundance.lean (频率坐标骨架, 无 sorry); fig11 数值验证 δ̄=0.9999 |
| 2026-08-25 | research | 能量无界已证: harmonic_sum_unbounded (调和发散 Lean 化, Hardy 侧); 均值定理桥 draft |
| 2026-08-26 | feat | §31 缺口 A 技术模板地基落地 (无 sorry 编译通过): rotVecOnLine/dirichletPartialSumLine 定义 (与 §19 旋转向量同族); integral_rotating_atom 自证 (∫exp(iΔx)=(e^{iΔT}−e^{−iΔT})/(iΔ); 因 mathlib module 系统迁移 integral_exp_mul_complex 暂不可见, 经 HasDerivAt.comp_ofReal + div_const + mul_div_cancel_right₀ 自建原函数论证); energy_single_term_integral (∫‖v_n‖²=2T(n+1)^{-2σ}, 经 norm_exp 纯虚模一 + rpow_add) — 对角能量项是 log 积累进入均值计算的精确位置; 待办: 双重和主展开 + 极限传递 |
| 2026-08-27 | fix | 修复 `paper/riemann-hibs` 中英文论文编译与版式：改用 XeLaTeX/xeCJK，移除 Babel 标点 shorthand 与错误的模板信息行属性；隐藏源码保留的 `\Lean{...}` 实现标识符，修复作者栏、长公式和参考文献字段。tectonic 干净构建中英文均成功，中文字符正常显示，原 reported 的 Babel、`There's no line here to end`、`Missing $ inserted` 与 BibTeX 字段错误均已消失 |
| 2026-08-27 | refactor | 重新组织双语论文的数学论证顺序：由经典输入出发，依次推出隐数坐标字典、反演不动圆、非平凡零点圆环定位、反射配对与 RH 等价约化；再把 Hardy 的已知定理直接传输到临界圆。能量和频率内容改为明确的辅助诊断，移除“定理组装独立证明无限零点”的误导性叙述；中英文源文件与 PDF 已重新编译 |
| 2026-08-27 | authorship | 双语论文补充隐数空间的前期工作与作者贡献说明：明确该坐标框架由论文作者在 `LiuXu2026bridge` 所对应的前期工作中提出并发展；正文补充半径、提升角坐标与叶编号的作用，并界定本篇是在既有构造上组织 $\zeta$ 零点论证，而非重新引入隐数空间 |
| 2026-08-27 | docs | 恢复论文重排时误删的经典文献引用：引言重新引用 Riemann、零点分布、Hardy 及其后续定量结果、对相关/谱观点、数值验证和标准专著；双语构建各生成 26 条参考文献 |
| 2026-08-27 | expansion | 扩充双语正文的解释性论证：增加隐数空间的覆盖表示与叶编号、反演不动圆的集合/点区分、圆环严格不等式来源、反射轨道与 RH 约化的双向说明、Hardy 传输中的提升含义、能量/频率诊断边界及 Lean 形式化层次说明；中英文干净构建通过 |
| 2026-08-27 | publish | 论文发表（英文版, 人类身份）: aixiv.science **aixiv.260827.000002** (Under Review, CC-BY-4.0, Formal Sciences→Mathematics→Number theory) + 以太坊 EAS 主网 attestation **0xab83532e8be8cf882db70583a8cc8e2a9f9bb38135b5183ef6b53dbda5010133** (复用 schema #405 `string ipfsCid, string title, bytes32 sha256`; IPFS CID QmYRqinyW2m6tpdN8g2TuysiMrQUK3srij7nGBQFMSJQtk 已本地 pin; sha256 ac5a565f...; 钱包 0xb4e9dCF7...; 验证 https://easscan.org/attestation/view/0xab83532e8be8cf882db70583a8cc8e2a9f9bb38135b5183ef6b53dbda5010133). 记录: ~/.hermes/eas-bind/README.md; 脚本 eas-bind-paper.js (通用版) |
| 2026-08-26 | research | draft 全部清理: 零点可数 (zero_set_countable) + 双射枚举 (zero_enumeration_of_infinite); Hardy 桥/丰度机制结构体化 (外部输入显式声明); 多值 log 单值化 (exp_log_sheet, 叶差 2πiℤ) |
| 2026-08-26 | research | B 桥原子层: 临界叶能量展开 Lean 形式化 (对角调和+交叉旋转积分+上界); 发现逐项上界过粗, 交叉相消=真实缺口; fig13 |
| 2026-08-26 | research | B1 交叉相消: 4 原子 Lean 化 (min 界/log 引理/近邻计数/分裂上界); 截断 X(t)=√(t/2π) 机制数值确认 (E/主项→0.5, 交叉/主项→0); fig13 v2 |
| 2026-08-26 | research | B3 隐数均值定理: 单侧能量 = (T/2)(log(T/2π)−1) + γ·T + o(T) — 对角积分 Lean 已证 + 欧拉常数项 Lean 已证 + 交叉次主导 (B1); 双侧重现 Hardy–Littlewood (2γ−1)T 项, 无需 Stirling |
| 2026-08-26 | research | 隐数坐标系 RH 完整实现: 机制环 + 半径唯一性骨架 (能量平衡半径 energy_balance_radius, 1/2=唯一平衡点) + B 桥 (截断交叉相消 + 隐数均值定理 E=(T/2)(log−1)+γT+o(T)) + 零点↔ℕ 闭环; 缺口 R1/R2 命名; paper 中英加第 11 节 + 诚实边界 (vi); fig14 |
| 2026-08-26 | feat | 论文中文同步版落地: `paper/Journal_of_the_French_Statistical_Society/riemann_hibs_zh.tex` — 与英文版(含 v2 新增 §11 balance + 诚实边界 (vi))完全同步: 82 label / 61 编号环境 (theorem 30, proposition 18, corollary 2, definition 11) / proof 3 / remark 15 / 112 个 Lean 名一一对应; 公式与引用 key 原样保留. XeLaTeX 路线编译通过 (tectonic 验证 0 error), 五处主文件级兼容补丁均不改 jsfds.cls/sty: ①去 microtype 伪加载(xelatex 下骗过 xeCJK 致崩); ②AddToHook{package/babel/after} 把 \shorthandon/:;?! 中和(jsfds.sty:2216 在 XeTeX 下报 shorthand 错); ③\Lean 重定义 \texttt{\detokenize{}}+标题内 \mbox 双保险(Lean 名下划线 catcode 8 在文本模式/moving arg 触发 Missing $); ④作者栏 \printcontact 改纯文本邮箱(XeTeX 下 printead/saferef 首编触发 Incomplete \iffalse); ⑤补 acknowledgement 环境定义(jsfds 类未内置). 附带发现: 英文版自身在 XeTeX 下同样过不了这些坑(\Abs 未定义/acknowledgement 未定义/下划线同病)——EN 版实际从未在 xelatex 成功编译. 中文版 xeCJKDeclareSubCJKBlock Greek 解决 keywords 裸 ζ 缺字形 |
| 2026-09-01 | fix | **§19 组装定位澄清 + RH 状态重新确立 (AI 日志夸大修正)**: ① log.md 2026-08-26 条目「§19 完整论证...⟹ 临界圆无限零点 (有位置+有丰度)」**夸大** — 对照 Abundance.lean: `CriticalCircleInfinity` 是**结构体骨架** (assemble 为字段组合声明, 无证明体), 组件中 R5 `alignment_density` 是猜想 (`FrequencyAlignmentConjecture` 开放字段), `bounded_energy_of_finite` 是 Hardy 外部输入, `infinite_on_circle` 无证明体; 临界圆无限零点的实际供给 = **Hardy 1914 的坐标化传输** (论文 §9 定位诚实, 非隐数新证明). ② R6 频率结构 (freq_gap_tendsto_zero / freq_gap_antitone / phase_freeze / fold_point_exists) 是**真定理** (Lean 已证), 但**只依赖高度 t, 不依赖半径 σ** — 不能区分圆上/离圆零点, 不构成零点存在或排除论证. ③ 推论6 订正数学正确 (独立核实): 「每个反射对都自配对」⟹ 零点∈{±√e} (self_pair_iff), 与 Hardy 无限零点冲突 — **假命题**; 「对整体落在圆上」⟺ ‖e/w‖=‖w‖ ⟺ ‖w‖=√e — **正确等价**. ④ **RH = `no_zero_off_circle` (圆环 1<‖w‖<e 无 ‖w‖≠√e 零点) 未验证**: R3 排 σ≥2, 函数方程反射排 σ<0 侧, **临界带 0<σ<1 完全开放**; R1-R6 均无「环内零点必须落圆」论证 (R6 与 σ 无关是结构性原因). 隐数坐标的贡献 = RH 的**精确等价重述** (机制环 A⟺B⟺C⟺D + no_zero_off_circle), 翻译完整但排除论证不存在 |
| 2026-09-01 | feat | **R7 Γ 不对称量 (缺口精确定位) + 推导式缺口分析落地**: leo 推导 — 已证清单按逻辑类型分类 (恒等式/丰度/临界性三类全有, 唯独缺「排除」= RH); 分部求和显示临界带部分和是外旋螺旋 (S_N→0 恒假, ζ=0 真身=螺旋中心是原点, 部分和→中心需解析延拓+余项估计=缺的桥); RS 截断 N=⌊√(t/2π)⌋≪折返点 n*≈t/π (t=1000: 12.6 vs 318) ⟹ fold_point_exists 落在发散尾部, 与零点联系未建立; 坐标双射=布尔代数同构不制造内容; **唯一 σ-不对称量** = Γ 比 \|Γ(s/2)\|/\|Γ((1−s)/2)\| ~ (t/2)^{σ−1/2} = (t/2)^{log(ρ/√e)} — 排除性论证必须经过它 (Stirling 级, mathlib 无). **Lean 落地 (Abundance §20, 无 sorry, lake build 2996 jobs 通过)**: `asym_exp_log_rho` (σ−1/2=log(ρ/√e) 坐标恒等式, 已证) + `gamma_reflection_mod` (反射公式模 \|Γ(s/2)\Gamma(1−s/2)\|=π/\|sin(πs/2)\|, 已证, mathlib Gamma_mul_Gamma_one_sub) + `GammaRatioAsymmetry` 结构体 (Stirling 比 = 显式外部输入, asymmetry_off_circle 缺口定位). 攻坚排序: A η 桥 (bookkeeping) / B σ>1/2 无零点 (=RH 墙) / C Γ 比定位 (✅已做). 完整推导见 current-status「缺什么：推导式定位」小节 |
| 2026-09-01 | feat | **A η 临界带桥 Lean 落地 (Abundance §21, 无 sorry, lake build 2996 jobs 通过)**: `etaPartialSum` (η 部分和 = 交错 × cpow, 对齐语言语法对象) + `eta_summable_of_one_lt_re` (**真定理**: Re>1 绝对收敛 — ‖项‖=(n+1)^{−σ} 经 norm_cpow_eq_rpow_re_of_pos + p 级数 Real.summable_nat_rpow_inv + comp_injective 平移) + `EtaCriticalStripBridge` 结构体 (临界带 0<σ<1 的条件收敛与延拓等式 η=(1−2^{1−s})ζ = 显式外部输入 — mathlib 无复数 Dirichlet/Abel 测试, 诚实标注). 对齐语言在临界带的良定 = Re>1 已证 + 两个显式输入; bookkeeping 完成, 不给 RH 加任何东西 |
| 2026-09-01 | feat | **B 墙侦察 + 骨架化 (Abundance §22, 无 sorry, lake build 通过)**: mathlib 家底确认 — Phragmén–Lindelöf ✅ (唯一性形态) / Borel–Carathéodory ✅ / 最大模 ✅; 零密度 / AFE / Riemann–Siegel / Lindelöf 估计 ❌ (真墙在 mathlib 外). **3-4-1 代数核心砖 (真定理)**: `three_four_one_nonneg` (3+4cosθ+cos2θ = 2(cosθ+1)² ≥ 0, cos_two_mul + nlinarith) + `mertens_coeff_re_nonneg` (Re(3+4(n+1)^{−it}+(n+1)^{−2it}) ≥ 0 — ζ(1+it)≠0 论证的构建块). **`ZeroFreeHalfPlane` 结构体**: 依赖链钉形状 — 已证 (函数方程反射 / σ≥2 零域 / 3-4-1 核心) + 外部输入 (条带增长上界 Lindelöf 级 / 零密度论证 — Hadamard–de la Vallée Poussin 型, 经典供给). B 供给清单入 current-status |
| 2026-09-01 | feat | **模平衡载体落地 (Abundance §23, 无 sorry, lake build 通过)**: `zeta_mod_balance` (|ζ(1−s)|=|χ(s)|·|ζ(s)|, 函数方程取模) + `chi_mod_decomp` (|χ(s)|=2(2π)^{−σ}|Γ(s)||cos(πs/2)|). χ 是唯一携带 σ-幂的传输因子, |χ(σ+it)|~C_σ t^{σ−1/2} (Stirling) — 离圆零点排除的候选载体. **关键张力发现**: 排除需 t-方向模估计, 但已有 gamma_ge_pow_mul_self 是 x-方向 (实轴) — 正交. **后续 = 复数 Γ 递推下界** |Γ(x+n+iy)| ≥ |y|^n·|Γ(x+iy)| (x-方向→t-方向), 不经 Stirling 进入模平衡. 探索地图入 current-status「内禀排除」小节 |
| 2026-09-01 | feat | **Γ 比精确恒等式 + 复数递推下界 (Abundance §26-27, 无 sorry, lake build 2996 jobs 通过)**: ①§26 `gamma_norm_ge_y_pow` — |Γ(x+n+iy)| ≥ |y|^n·|Γ(x+iy)| (x>0), gamma_ge_pow_mul_self 的复推广 (x-方向→t-方向), 每个因子 |x+k+iy| ≥ |y|; ②§27 `gamma_ratio_exact` — Γ(s/2)/Γ((1−s)/2) = √π/(2^s·sin(πs/2)·Γ(1−s)), 由反射 (Gamma_mul_Gamma_one_sub) + 加倍 (Gamma_mul_Gamma_add_half) 精确推出, **无 Stirling**. 意义: R7 的 Γ 比 (Stirling 渐近 ~(t/2)^{σ−1/2}) 现在有精确代数载体 — 取模后 |Γ(s/2)|/|Γ((1−s)/2)| = √π/(2^σ·|sin(πs/2)|·|Γ(1−s)|). 排除论证的完整代数骨架 = 模平衡 (§23) + χ 分解 + 本 Γ 比精确式, 剩余缺口 = sin/Γ(1−s) 的 t-方向模估计 (并行会话在攻 η 条件收敛+Abel 求和 [A] 组件) |
| 2026-09-01 | feat | **χ 精确化 (Abundance §29, 无 sorry, lake build 2996 jobs 通过)**: `chi_eq_pi_div_sin_gamma` (χ(s) = (2π)^{−s}·π/(sin(πs/2)·Γ(1−s)), 反射 Gamma_mul_Gamma_one_sub + 倍角 sin_two_mul) + `chi_mod_sin_gamma` (|χ(s)| = (2π)^{−σ}·π/(‖sin(πs/2)‖·‖Γ(1−s)‖), 取模). **意义**: χ 的 σ-幂 (Stirling 渐近 ~t^{σ−1/2}) 现在有精确代数载体 — sin/Γ(1−s) 的模是初等可估的 (sinh/cosh 显式), 从 Stirling 渐近走向精确排除载体的关键一步. 结合模平衡 (§23) 得 |ζ(1−s)| = π(2π)^{−σ}/(‖sin(πs/2)‖‖Γ(1−s)‖)·|ζ(s)|. 并行会话 §28 (内禀排除判据) 的 chi_asymptotic 字段正需此载体; 剩余缺口 = sin/Γ(1−s) 的 t-方向模估计 (‖sin(πs/2)‖² = sinh²(πt/2)+sin²(πσ/2), Γ 用递推下界/反射) |
| 2026-09-01 | research | **「最后一步」精确定位 (e 指数抵消)**: 排除论证完整链条已精确化且无 Stirling — ①模平衡 |ζ(1−s)|=|χ(s)||ζ(s)| (§23) ②χ 精确化 |χ(s)|=π(2π)^{−σ}/(‖sin(πs/2)‖‖Γ(1−s)‖) (§29) ③Γ 比精确 (§27) ④复数递推下界 (§26). **sin/cos 显式的一半已现成**: mathlib 有 `Complex.sin_mul_I` (sin(x·I)=sinh x·I) / `Complex.cos_mul_I` (cos(x·I)=cosh x). **两个剩余缺口**: (i) ‖cos(x+iy)‖²=cos²x+sinh²y 的显式需要 Complex.ofReal 三角关系 (`Complex.cos_ofReal`/`cosh_ofReal` 等 mathlib 缺失, 卡在 re/im 提取); (ii) **最后一道墙** = ‖Γ(s)‖ 的 Stirling t-渐近 ~t^{σ−1/2}e^{−πt/2} — 与 ‖cos(πs/2)‖ 的 e^{πt/2} 增长 (sinh) 相抵消, 留下 t^{σ−1/2} 幂 = |χ| 的 σ-不对称. mathlib 无 Stirling, 无法真证. **排除论证到此: 代数骨架精确完成, 唯剩 Γ 的 t-方向模估计 (Stirling 级) 一道输入** |
| 2026-09-01 | feat | **ofReal 三角补丁完成 (Abundance §31, 无 sorry, lake build 3217 jobs 通过)**: `cos_norm_sq` (‖cos(x+iy)‖² = cos²x + sinh²y) + `sin_norm_sq` (‖sin(x+iy)‖² = sin²x + sinh²y) — 用 mathlib 现成的 `Complex.cos_eq`/`sin_eq` + `ofReal_cos/sin/cosh/sinh` (此前误判为缺口, 实为引理名不同). 消除「中间缺口」: sin/cos 模显式成立, ‖cos(πs/2)‖² = cos²(πσ/2)+sinh²(πt/2) 可估 (增长 ~e^{πt/2}); 最后一道墙精确孤立为 ‖Γ(s)‖ 的 Stirling t-渐近 (e^{−πt/2} 衰减) — 与 sinh 的 e^{πt/2} 抵消留 t^{σ−1/2} |
